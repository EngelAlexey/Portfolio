---
slug: kaizen-ai
title: Kaizen AI | Inteligencia de negocio
tagline: Las empresas que licencian el ERP consultan sus propios datos escribiendo la pregunta. El servidor acota cada consulta a lo que esa empresa y ese rol pueden ver.
areas: [ia, seguridad]
kind: profesional
org: Kaizen Apps CR
role: Desarrollo
period:
  start: '2025-09'
  end: '2026-07'
tier: ficha
home: true
visibility: privado
repo: null
site: null
stack:
  - Next.js
  - TypeScript
  - Vercel AI SDK
  - Upstash Vector
  - Cohere rerank
  - Drizzle ORM
  - MySQL
  - Google Cloud Run
cover: null
order: 2
diagram:
  nodes:
    - "Pregunta"
    - "Modelo"
    - "SQL propuesto"
    - "Reescritor AST"
    - "SQL ejecutado"
    - "Datos permitidos"
  gate: 3
  layers:
    - name: "Base de datos del cliente"
      body: "Cada consulta queda acotada a la base del cliente que pregunta. Un usuario de una empresa no alcanza los datos de otra, aunque el SQL generado los pidiera de forma explícita."
    - name: "Rol de la sesión"
      body: "Desarrollador, administrador y usuario ven conjuntos distintos. El filtro se inyecta según el rol autenticado, no según lo que el modelo proponga."
    - name: "Alcance de la acción"
      body: "La acción concreta acota el resultado una vez más. Lo que el modelo devuelve es una intención; el alcance efectivo lo fija el servidor."
    - name: "Registro de la consulta"
      body: "Queda registrado el SQL que se ejecutó, junto con el rol y el usuario. La auditoría se hace sobre la consulta final, no sobre la que el modelo propuso."
  caption: "El reescritor actúa entre el SQL que propone el modelo y el que llega a la base."
---

import ArchDiagram from '../../../components/ArchDiagram.astro';

## Contexto

Kaizen licencia un ERP de recursos humanos a otras empresas. Kaizen AI es la herramienta que pone en manos de esas empresas. Consultan sus propios datos escribiendo la pregunta en lenguaje natural, sin pasar por un informe ni por el equipo de soporte.

El proyecto nació de una necesidad de la operación, no de una idea de producto: el equipo de soporte atendía una a una las consultas de todos los clientes, y ese trabajo crecía al mismo ritmo que la cartera.

## Problema

Cada consulta de un cliente la respondía una persona del equipo de soporte. Conforme crece la demanda de ayuda, ese equipo se satura y el cliente espera.

Buena parte de esas consultas se repiten: cómo se hace algo, qué significa un concepto de recursos humanos o de salud ocupacional, qué dice la legislación laboral al respecto. Eran horas de un equipo especializado dedicadas a preguntas que ya tenían respuesta.

## Decisiones técnicas

Varias empresas cliente usan la herramienta sobre la misma instalación. Eso convierte el control de acceso en la decisión central del proyecto: un modelo que consulta datos productivos plantea una pregunta que un prompt no resuelve, que es quién puede leer qué. Si la restricción dependiera de que el modelo siga instrucciones, bastaría reformular la pregunta para saltársela.

La pregunta se traduce a SQL y el servidor reescribe esa consulta antes de ejecutarla. Un reescritor sobre el árbol sintáctico inyecta tres filtros: base de datos del cliente, rol de la sesión y alcance de la acción.

Una consulta que pidiera datos de otra empresa no falla una validación. Se ejecuta ya acotada y devuelve solo lo permitido, que es una garantía más fuerte que rechazarla.

La versión en producción es la cuarta. Cada reconstrucción movió al código una decisión que la anterior dejaba en manos del modelo.

## Arquitectura

El modelo no habla con la base. Propone un SQL, y ese texto pasa por el reescritor antes de ejecutarse.

<ArchDiagram {...frontmatter.diagram} />

El reescritor es el único punto del recorrido que fija cuánto se devuelve, así que las pruebas de permisos se escriben contra él y no contra la conversación.

## Resultado

La herramienta está en producción para las empresas que licencian el ERP. Cada una consulta sus propios datos, distingue lo que ve un desarrollador, un administrador y un usuario, oculta los campos sensibles y deja registro de cada consulta ejecutada.

Las preguntas que antes ocupaban al equipo de soporte las resuelve ahora el cliente, y el equipo atiende lo que sí necesita a una persona.

La recuperación exige un mínimo de relevancia. Una pregunta sin respaldo en los datos devuelve que no hay información, en lugar de un texto armado con fragmentos poco relacionados.

## Lo que aprendí

El control de visibilidad empezó apoyado en el texto de la instrucción y terminó entero en el código. La diferencia práctica es que ahora se puede ejecutar: una prueba pasa una pregunta por el reescritor y comprueba que el SQL resultante lleva los tres filtros.

Sobre un prompt no hay nada equivalente que ejecutar. Cuando una garantía tiene que sostenerse frente a un cliente, conviene ponerla donde una prueba pueda comprobarla.
