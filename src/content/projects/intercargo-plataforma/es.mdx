---
slug: intercargo-plataforma
title: Plataforma de Intercargo | ERP interno y sitio público
tagline: Sitio público, portal interno, debida diligencia de clientes y cuentas por cobrar. El sitio abierto y el portal corren separados, con permisos distintos.
areas: [seguridad, fullstack]
kind: profesional
org: Intercargo Panamá — vía Kaizen Apps CR
role: Desarrollo y modelo de seguridad
period:
  start: '2026-07'
  end: null
tier: ficha
home: true
visibility: privado
repo: null
site: null
stack:
  - Next.js
  - React
  - TypeScript
  - MySQL
  - Google Cloud Run
  - Docker
  - Vitest
  - Playwright
  - Gemini
cover: null
order: 1
diagram:
  nodes:
    - "Visitante"
    - "Sitio público"
    - "Invitación"
    - "Portal"
    - "Datos"
  gate: 3
  layers:
    - name: "Separación de instancias"
      body: "La misma imagen se despliega dos veces con identidades distintas. Al servicio público se le retira el acceso a la base de datos, así que un fallo en el sitio abierto no tiene ruta hacia lo que guarda el portal."
    - name: "Identidad por invitación"
      body: "No hay registro abierto ni contraseñas propias. Se entra por invitación y con un solo proveedor de identidad. Las sesiones se revocan desde el servidor, no solo caducan."
    - name: "Cotas antes del modelo"
      body: "Tamaño de archivo, peticiones por origen y validación de tipo se aplican antes de que nada llegue al modelo. El límite lo fija el servidor; el modelo no decide cuánto puede pedir."
    - name: "Verificación antes de publicar"
      body: "El pipeline corre tipos y pruebas, construye la imagen y la escanea. Una vulnerabilidad crítica aborta el despliegue antes de que exista un servicio nuevo."
  caption: "La compuerta la impone el despliegue: dos servicios de la misma imagen, con permisos distintos."
---

import ArchDiagram from '../../../components/ArchDiagram.astro';

## Contexto

La plataforma reúne el sitio público de Intercargo y el portal donde trabaja su personal: debida diligencia de clientes, cuentas por cobrar, nómina y seguimiento de las solicitudes que entran por el sitio. Está en desarrollo activo y crece por módulos.

El encargo original fue el portal interno. La propuesta de rehacer también el sitio público salió del equipo. Mover ambos a una infraestructura propia era la única forma de sostener el portal con las garantías que el cliente pedía. Entré a construir sobre la plataforma y a definir su modelo de seguridad.

## Problema

El sitio corría sobre un servicio de terceros que el equipo no administraba. Sin control sobre esa infraestructura no había manera de alojar junto a él un portal con documentos de clientes.

La debida diligencia se resolvía leyendo esos documentos a mano, expediente por expediente. Cada alta consumía tiempo de una persona antes de que el cliente pudiera operar, y el ritmo de altas dependía de cuánta gente hubiera disponible para revisarlas.

## Decisiones técnicas

Un sitio de marketing y un portal con documentos de clientes tienen exposiciones opuestas. Servirlos desde el mismo proceso convertiría cualquier fallo del primero en un acceso al segundo.

La frontera quedó en el despliegue y no en el código. Una sola imagen se publica como dos servicios con identidades separadas, y al servicio público se le retira el acceso a la base de datos.

La segunda regla es que el código de servidor nunca se importa desde el navegador, ni siquiera para tipos. Cuando el cliente importó una constante desde un módulo de servidor, el grafo de dependencias arrastró consigo el SDK del modelo: 269 KB de JavaScript inútil en cada visita. No se expuso ninguna credencial, porque el framework solo publica las variables con prefijo público, pero el peso era real. La regla se comprueba ahora con un comando dentro del pipeline.

## Arquitectura

Los dos servicios comparten imagen y binario. Lo que no comparten es la identidad con la que corren, y de ahí sale todo lo demás.

<ArchDiagram {...frontmatter.diagram} />

Un documento subido por un cliente pasa por los límites de tamaño, tipo y frecuencia antes de llegar al modelo. Lo que el modelo devuelve se valida contra el esquema antes de escribirse, así que una extracción mal formada no llega a la base.

## Resultado

La plataforma sirve el sitio público, el portal interno, la debida diligencia y las cuentas por cobrar desde dos servicios con permisos distintos. La revisión documental que antes ocupaba a una persona ahora la propone el sistema, y la persona confirma o corrige.

La verificación acompaña a cada despliegue: 2 243 pruebas unitarias en 144 archivos y 27 suites end to end. Cuatro de ellas son suites de ataque dedicadas, una por módulo: autenticación, cuentas por cobrar, debida diligencia y sitio público.

## Lo que aprendí

Separar por despliegue cuesta más que una comprobación dentro del código. Son dos servicios que configurar, dos identidades que mantener y un despliegue que puede quedar a medias entre versiones.

A cambio, la garantía deja de depender de que la aplicación se comporte como se espera. Si el sitio público llegara a ejecutar código arbitrario, seguiría sin tener credenciales de base de datos que usar. Para un portal que guarda documentos de clientes, esa diferencia justifica el coste de operar dos servicios.
