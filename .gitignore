node_modules

# Build output
dist
.astro

# Deploy
.vercel

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Astro
astro.config.mjs.timestamp-*

# Working material for the redesign: generators, brand artwork and canvases.
# Not part of the build, and it holds client-owned source art.
design/

# public/cv/*.pdf is committed on purpose: the deploy host cannot reach the CV
# repo, so the PDFs have to travel with this one.
